# AI-Powered-Academic-Discovery-Agent-
Built a retrieval-augmented generation (RAG) agent that ingests academic paper corpora, generates dense vector embeddings via Hugging Face transformer models, and stores them in ChromaDB for sub-second semantic similarity search.
